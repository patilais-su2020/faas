version: 0.0
Resources:
  - lambda-webapp:
      Type: AWS::Lambda::Function
      Properties:
        Name: "lambda-webapp"
        Alias: "lambda-webapp"
        CurrentVersion: "1"
        TargetVersion: "2"